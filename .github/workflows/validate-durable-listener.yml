name: Validate provider Auth fault cleanup

on:
  push:
    branches:
      - agent/validate-durable-listener-v2

permissions:
  contents: write

concurrency:
  group: validate-provider-auth-fault-cleanup
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out validator branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Reset to exact rs base
        run: |
          cp .github/scripts/remove_provider_fault_injection.py /tmp/remove_provider_fault_injection.py
          cp .github/scripts/validate_provider_fault_cleanup.sh /tmp/validate_provider_fault_cleanup.sh
          git reset --hard ba68692718ca2020561f7cfc666c402232a3dfc3

      - name: Publish running run id
        run: |
          git config user.name "trellis-validation"
          git config user.email "actions@users.noreply.github.com"
          printf '%s\n' '${{ github.run_id }}' > .validation-running
          git add .validation-running
          git commit -m "Record provider Auth cleanup validation run"
          git push --force origin HEAD:refs/heads/agent/running-provider-fault-cleanup-v1
          git reset --hard ba68692718ca2020561f7cfc666c402232a3dfc3

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: 2.9.4

      - name: Cache generator build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: rust/tools/generate -> target

      - name: Validate provider Auth cleanup
        run: bash /tmp/validate_provider_fault_cleanup.sh

      - name: Publish failure run id
        if: failure()
        run: |
          git reset --hard ba68692718ca2020561f7cfc666c402232a3dfc3
          git config user.name "trellis-validation"
          git config user.email "actions@users.noreply.github.com"
          printf '%s\n' '${{ github.run_id }}' > .validation-failed
          git add .validation-failed
          git commit -m "Record failed provider Auth cleanup validation"
          git push --force origin HEAD:refs/heads/agent/failed-provider-fault-cleanup-v1
