[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
miette = { version = "7.6.0", features = ["fancy"] }
trellis-generate-runner = { path = "../crates/generate-runner" }
wasm-bindgen-cli-support = "=0.2.125"
