{
  "name": "@qlever-llc/trellis-app-console",
  "version": "0.0.0",
  "exports": "./contract.ts",
  "extends": "../../deno.json",
  "imports": {
    "@trellis/apis/trellis.auth": "./.trellis/generated/ts/trellis-apis/trellis.auth.ts",
    "@trellis/apis/trellis.core": "./.trellis/generated/ts/trellis-apis/trellis.core.ts",
    "@trellis/apis/trellis.eventlog": "./.trellis/generated/ts/trellis-apis/trellis.eventlog.ts",
    "@trellis/apis/trellis.health": "./.trellis/generated/ts/trellis-apis/trellis.health.ts",
    "@trellis/apis/trellis.jobs": "./.trellis/generated/ts/trellis-apis/trellis.jobs.ts",
    "@trellis/apis/trellis.state": "./.trellis/generated/ts/trellis-apis/trellis.state.ts"
  },
  "tasks": {
    "dev": "deno task -c ../../deno.json install && deno task -c ../../deno.json protocol:wasm && deno run -A vite dev",
    "build": "deno task -c ../../deno.json install && deno task -c ../../deno.json protocol:wasm && deno run -A vite build",
    "preview": "deno run -A vite preview",
    "check": "deno task -c ../../deno.json install && deno task -c ../../deno.json protocol:wasm && deno run -A @sveltejs/kit sync && deno run -A svelte-check --tsconfig ./tsconfig.check.json"
  },
  "compilerOptions": {
    "types": ["npm:vite/client"]
  },
  "lint": {
    "rules": {
      "exclude": ["import-map-remap"]
    }
  }
}
