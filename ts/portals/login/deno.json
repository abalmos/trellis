{
  "name": "@qlever-llc/trellis-portal-login",
  "version": "0.0.0",
  "exports": "./contract.ts",
  "extends": "../../deno.json",
  "tasks": {
    "prepare": "deno task -c ../../deno.json prepare",
    "dev": "deno task prepare && deno run -A vite dev",
    "build:static": "deno task prepare && deno run -A @sveltejs/kit sync && deno run -A vite build",
    "build:static:prebuilt": "deno run -A --lock=../../deno.lock --frozen @sveltejs/kit sync && deno run -A --lock=../../deno.lock --frozen vite build",
    "build:embedded": "deno run -A scripts/build_embedded.ts",
    "build": "deno task prepare && deno run -A vite build",
    "preview": "deno task prepare && deno run -A vite preview",
    "check": "deno task prepare && deno run -A @sveltejs/kit sync && deno run -A svelte-check --tsconfig ./tsconfig.check.json"
  },
  "compilerOptions": {
    "types": [
      "npm:vite/client"
    ]
  }
}
