# Generated by Trellis.

Package `service-trellis` contains `apis` and `participants` modules. Use an ordinary Cargo path dependency; regeneration is not part of a consumer build.

## API `demo.service@v1`
- RPC `Assignments.List`: descriptor `service_trellis::apis::demo_service::rpc::AssignmentsListRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::AssignmentsListRpc>(...)`, generated wrapper `.rpc().assignments().list(...)`
- RPC `Evidence.Delete`: descriptor `service_trellis::apis::demo_service::rpc::EvidenceDeleteRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::EvidenceDeleteRpc>(...)`, generated wrapper `.rpc().evidence().delete(...)`
- RPC `Evidence.Download`: descriptor `service_trellis::apis::demo_service::rpc::EvidenceDownloadRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::EvidenceDownloadRpc>(...)`, generated wrapper `.rpc().evidence().download(...)`
- RPC `Evidence.List`: descriptor `service_trellis::apis::demo_service::rpc::EvidenceListRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::EvidenceListRpc>(...)`, generated wrapper `.rpc().evidence().list(...)`
- RPC `Reports.List`: descriptor `service_trellis::apis::demo_service::rpc::ReportsListRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::ReportsListRpc>(...)`, generated wrapper `.rpc().reports().list(...)`
- RPC `Sites.Get`: descriptor `service_trellis::apis::demo_service::rpc::SitesGetRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::SitesGetRpc>(...)`, generated wrapper `.rpc().sites().get(...)`
- RPC `Sites.List`: descriptor `service_trellis::apis::demo_service::rpc::SitesListRpc`, low-level `trellis_client.call::<service_trellis::apis::demo_service::rpc::SitesListRpc>(...)`, generated wrapper `.rpc().sites().list(...)`
- Event `Audit.Recorded`: `trellis_client.publish::<service_trellis::apis::demo_service::events::AuditRecordedEventDescriptor>(...)`, generated wrapper `.event().audit().recorded().publish(...)`, prepare with `trellis_client.prepare_event::<service_trellis::apis::demo_service::events::AuditRecordedEventDescriptor>(...)`
- Event `Evidence.Uploaded`: `trellis_client.publish::<service_trellis::apis::demo_service::events::EvidenceUploadedEventDescriptor>(...)`, generated wrapper `.event().evidence().uploaded().publish(...)`, prepare with `trellis_client.prepare_event::<service_trellis::apis::demo_service::events::EvidenceUploadedEventDescriptor>(...)`
- Event `Reports.Published`: `trellis_client.publish::<service_trellis::apis::demo_service::events::ReportsPublishedEventDescriptor>(...)`, generated wrapper `.event().reports().published().publish(...)`, prepare with `trellis_client.prepare_event::<service_trellis::apis::demo_service::events::ReportsPublishedEventDescriptor>(...)`
- Event `Sites.Refreshed`: `trellis_client.publish::<service_trellis::apis::demo_service::events::SitesRefreshedEventDescriptor>(...)`, generated wrapper `.event().sites().refreshed().publish(...)`, prepare with `trellis_client.prepare_event::<service_trellis::apis::demo_service::events::SitesRefreshedEventDescriptor>(...)`
- Feed `Audit.Feed`: `trellis_client.feed::<service_trellis::apis::demo_service::feeds::AuditFeedFeedDescriptor>(input)`, generated wrapper `.feed().audit().feed(...)`
- Operation `Evidence.Upload`: `trellis_client.operation::<service_trellis::apis::demo_service::operations::EvidenceUploadOperation>().start(...)`, generated wrapper `.operation().evidence().upload().start(...)`
- Operation `Reports.Generate`: `trellis_client.operation::<service_trellis::apis::demo_service::operations::ReportsGenerateOperation>().start(...)`, generated wrapper `.operation().reports().generate().start(...)`
- Operation `Sites.Refresh`: `trellis_client.operation::<service_trellis::apis::demo_service::operations::SitesRefreshOperation>().start(...)`, generated wrapper `.operation().sites().refresh().start(...)`
## Participant `demo.service@v1`
Connect through `service_trellis::participants::demo_service::Participant`.

Prepared events and outbox/inbox:
- Generated event structs are event bodies only; runtime metadata is separate from the body payload.
- `PreparedTrellisEvent` captures a validated subject, encoded body payload, preserved transport headers, event id, and event time.
- Published prepared events send the runtime event id from `event_id()` and `Trellis-Event-Time` from `event_time()`.
- Use `subscribe_messages::<Descriptor>(...)` and `EventMessage::event_id()` / `event_time()` when subscribers need metadata.
- Use `prepare_event::<Descriptor>(...)`, `publish_prepared(...)`, and `dispatch_outbox_once(...)` for durable publish flows.
- Runtime stores include `OutboxStore`, `InboxStore`, `SqliteOutboxStore`, `SqliteInboxStore`, `PostgresOutboxStore`, and `PostgresInboxStore`.

