# Type system and errors

Trellis IDL is the authoring language for wire schemas. JSON Schema is the current canonical lowering, not the source language or permanent backend contract.

Public object schemas are additive and tolerate unknown members. Typed maps use `map<T>`. Expected RPC and operation failures are declared error values and are returned through generated `Result` surfaces; exceptions remain reserved for unexpected runtime failures.
